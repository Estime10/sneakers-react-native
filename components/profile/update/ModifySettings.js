import { View, Text } from 'react-native'
import React from 'react'

const ModifySettings = ({ navigation }) => {
  return (
    <View>
      <Text>ModifySettings</Text>
    </View>
  )
}

export default ModifySettings
